FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .

ENV VITE_API_BASE_URL=http://recsys-backend:5001

EXPOSE 3005

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3005"]
